<!DOCTYPE html>
<html>
<head>
  <title>LightningApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <%= image_tag "lightning.png", class: "logo" %>
          <% if current_user %>
            <%= link_to "Sign Out", signout_path, method: :delete, class: "btn btn-default navbar-btn" %>
            <% else %>
              <%= link_to "Sign In with GitHub", "auth/github", class: "btn btn-default navbar-btn" %>
          <% end %>
        </div>
      </div>
    </nav>
    
  <% if current_user.nil? %>
  <div class="jumbotron">
    <h1>g7 Lightning Talk App</h1>
    <p>If you're trying to sign up for a lightning talk, you've come to the right place.<p>
  </div>
  <% end %>
    <%= yield %>
  </div>
</body>
</html>
